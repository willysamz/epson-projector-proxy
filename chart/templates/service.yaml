apiVersion: v1
kind: Service
metadata:
  name: {{ include "epson-projector-proxy.fullname" . }}
  labels:
    {{- include "epson-projector-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "epson-projector-proxy.selectorLabels" . | nindent 4 }}
